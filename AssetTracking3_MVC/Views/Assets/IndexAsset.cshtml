@model List<AssetItem>
@*
    This page display all Asset database
*@
@{
    ViewData["Title"] = "Asset Data Maintenance";
}
<h1>@ViewData["Title"]</h1>
<div>
    <div class="row pt-4">
        <div class="col-9">
        <h2 class="text-success">Recently Added Asset Items</h2>
        </div>
        <div class="col-3">
            <a asp-controller="Assets" asp-action="CreateAsset" class="btn btn-primary btn-lg">Add New Item</a>
        </div>
    </div>
</div>
<br />

<table class="table table-bordered table-hover table-striped"> 
    <thead>
        <tr>
            <th>Asset ID</th>
            <th>Country</th>
            <th>Category</th>
            <th>Price</th>
        </tr>
    </thead>

    <tbody>
 @*       @foreach(var item in Model){
            <tr>
                <td width="10%">@item.Id</td>
                <td width="20%">Asset office</td>
                <td width="10%">Asset category</td>
                <td width="10%">Asset price</td>
                <td width="30%">
                    <div class="w-100 bth-group" role="group">
                        <a asp-controller="Assets" asp-action="UpdateAsset" app-route-id ="@item.Id" class="btn btn-primary">Update</a>
                        <a asp-controller="Assets" asp-action="DeleteAsset" app-route-id ="@item.Id" class="btn btn-danger">Delete</a>

                    </div>
                </td>
            </tr>
        }*@
     </tbody>
 </table>